import fs from 'fs'
import { ChildProcess } from 'child_process'

// const writeStream = fs.createWriteStream('./Data streams.txt');

// process.stdin.pipe(writeStream);
// process.stderr.write('Error occured while writing data!')



